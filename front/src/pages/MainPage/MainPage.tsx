import { FC } from "react";
import styles from "./MainPage.module.scss";

const MainPage: FC = () => {
  return (
    <div className={styles.container}>
      <div className={styles.title}>App Shop Main Page</div>
    </div>
  );
};

export default MainPage;
